[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ada"
version = "0.1.0"
description = "Agentic database assistant"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langfuse",
    "openai",
    "pydantic",
]
